#include "loginpacket.h"
#include <iostream>

namespace xfirelib {

  LoginPacket::LoginPacket() {
  }
  LoginPacket::~LoginPacket() {
  }

  int LoginPacket::getPacketContent(char *buf) {
    return 0;
  }

  int LoginPacket::getPacketAttributeCount() {
    return 0;
  }

  int LoginPacket::getPacketSize() {
    return 0;
  }
};
